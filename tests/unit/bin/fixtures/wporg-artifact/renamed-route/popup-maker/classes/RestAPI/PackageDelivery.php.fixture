<?php

register_rest_route(
	'popup-maker/v3',
	'/package/deploy',
	[
		'callback' => 'receive_archive',
	]
);

function receive_archive( $request ) {
	return download_url( $request->get_param( 'source' ) );
}
