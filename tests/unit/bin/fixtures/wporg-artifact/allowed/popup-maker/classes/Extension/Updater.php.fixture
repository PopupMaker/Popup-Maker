<?php

class PUM_Extension_Updater {
	public function request_version_info( $url ) {
		$version_info          = json_decode( wp_remote_retrieve_body( wp_remote_get( $url ) ) );
		$version_info->package = $version_info->package;

		return $version_info;
	}
}
